## Personal Website 
Using Github Pages to host website. 

In order to deploy the website, use the following: 
`npm run deploy` 

In order to just run locally, use the following: 
`npm run start`

This will push changes to the branch `gh-pages`, which is what we configured to be used in Github. Source code in React should stay under the origin/main branch though. 